#!/usr/bin/env bash
# scripts/run_benchmarks.sh
#
# Runs cmd/loadtest once per algorithm against the two local gateway
# instances, writes output into results/, and prints the summary table
# used in the README.
